# パスポート写真が保存されない不具合の修正

## 対象ファイル
- `lib/guestBooking.ts`（1ファイルのみ変更）

## 原因（調査で確定）
Supabaseの容量・設定・ストレージ権限はすべて正常でした（Storage 47MB/1GB、
バケットは公開、ポリシーも適用済み）。ゲスト92人中83人（約90%）は写真あり。
写真が無いのは9人だけで、6月6人・7月0人・8月3人という分布でした。

原因は、予約時のパスポート写真アップロードが**一時的に失敗したとき**、
コードがそのエラーを黙って握りつぶし、写真なしのまま予約を完了させていたこと。
リトライも無く、代表者でも写真欠落を許してしまっていました。

## 修正内容
1. **アップロードの自動リトライ（最大3回・指数バックオフ）**
   一時的な通信不調による取りこぼしを吸収する。
2. **失敗を明示化**
   `fetch`失敗・空blob・ストレージエラー・公開URL取得失敗をそれぞれ例外にし、
   握りつぶしをやめた。
3. **代表者は写真必須に**
   代表者の写真がリトライしても保存できない場合は、予約を黙って完了させず
   エラーを出して撮り直し・再送信を促す（`saveGuestEntry(entry, requirePhoto)`）。
   同行者は従来どおり予約は止めず、失敗はログに残す。

DB（テーブル／RLS／ストレージ）側の変更は不要です。コードの差し替えのみで有効になります。

## 反映手順（GitHub Web）
1. リポジトリで `lib/guestBooking.ts` を開く
2. 鉛筆（Edit）アイコン → 中身を同梱の `lib/guestBooking.ts` の内容で全置換
3. Commit → 通常どおり再デプロイ

`git` が使える場合は同梱の `guestBooking.patch` をリポジトリ直下で
`git apply guestBooking.patch` でも適用できます。

## 既存の欠落分について
既に写真が無い9人ぶんは、アップロード自体が行われず保存されていないため
復元はできません（今回の修正は「次回以降の予約が正常に保存される」ための対策です）。
