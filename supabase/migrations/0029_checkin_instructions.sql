-- =========================================================
-- セルフチェックイン案内
-- 宿ごとに、鍵の受け取り方・入館手順などの案内文を保持する。
-- 予約確定後にゲストの「旅程」画面へ表示される。
-- =========================================================
alter table stays_listings
  add column if not exists checkin_instructions text;
